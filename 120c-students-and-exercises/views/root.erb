<ul>
  <% @students.each do |student| %>
    <li><%= student.first_name %> <%= student.last_name %></li>
      <ul>
        <% @exercises.each do |exercise| %>
          <li><%= exercise.directory %></li>
        <% end %>
      </ul>
  <% end %>
</ul>

<%# ask Daniel about "<% vs. <%=". Also ask about "order of operations" 
#when combining html and Ruby syntax and "<%end..." - when you need to use it %>
